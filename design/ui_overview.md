# UI Overview

Goals:
- Sports broadcast vibe (ESPN-style overlay).
- Clean HUD usable by kids + adults.
- Minimal clutter during free roam.

HUD:
- Top center banner:
  - Free roam: city + zone name.
  - In challenge: `SPORT – Challenge` + timer.
- Top right mini panel:
  - In challenge: `Score: <value>`.
- Bottom left:
  - Player card with rank, XP bar, coins.
- Bottom center:
  - Context actions: `E` interact / start challenge, `Tab` scoreboard, `G` emotes, `C` quick chat.
- Bottom right:
  - Emote + quick chat hints.

Scoreboard (Tab):
- Simple table of players (for future global/zone boards; v1 can show placeholder or personal bests).
